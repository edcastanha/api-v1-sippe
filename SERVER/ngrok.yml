authtoken: 2X0lUS4omvUwTIgXmYrjyHi523X_6WVNoX5LoYnynjKH2MUjh
version: 2
tunnels:
  secedu:
    proto: http
    domain: distinctly-composed-redbird.ngrok-free.app
    #domain: secedu.com.br
    addr: 127.0.0.1:7000
    basic_auth:
      - secedu_1:secedu_1
      - secedu_2:secedu_2
    
    # -------------------------
    # Additional options
    # -------------------------
    # auth: "username:password"
    # host_header: rewrite
    # inspect: true
    # bind_tls: true

# ngrok start --all
# ngrok http --domain=distinctly-composed-redbird.ngrok-free.app 7000